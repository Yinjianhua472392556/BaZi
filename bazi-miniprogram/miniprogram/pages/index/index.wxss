/**index.wxss**/

/* 页面容器 */
.container {
  min-height: 100vh;
  background: linear-gradient(180deg, #E8F5E8 0%, #F0F8FF 50%, #FFF8DC 100%);
  position: relative;
}

/* 简化的页面头部 */
.page-header {
  padding: 20rpx 30rpx 20rpx;
  position: relative;
  z-index: 10;
}

.page-title-container {
  text-align: center;
  margin-bottom: 10rpx;
}

.page-title {
  font-size: 42rpx;
  font-weight: bold;
  color: #2F4F4F;
  margin-bottom: 8rpx;
  text-shadow: 0 2rpx 4rpx rgba(255, 255, 255, 0.8);
}

.page-subtitle {
  font-size: 26rpx;
  color: #666;
  opacity: 0.9;
}

.page-decoration {
  text-align: center;
  font-size: 32rpx;
  color: #C8860D;
  opacity: 0.8;
}

/* 主要内容区域 */
.main-content {
  padding: 40rpx 30rpx;
  background: white;
  margin: -30rpx 20rpx 0;
  border-radius: 30rpx;
  box-shadow: 0 -10rpx 30rpx rgba(0, 0, 0, 0.1);
  position: relative;
  z-index: 10;
}


/* 卡片样式更新 */
.card {
  background: white;
  border-radius: 20rpx;
  padding: 30rpx;
  margin: 20rpx 0;
  box-shadow: 0 4rpx 15rpx rgba(0, 0, 0, 0.1);
  border: 1rpx solid #F0F0F0;
}

/* 按钮样式更新 */
.btn-primary {
  background: linear-gradient(135deg, #C8860D 0%, #DAA520 100%) !important;
  color: white !important;
  font-size: 36rpx;
  font-weight: bold;
  padding: 25rpx;
  border-radius: 50rpx;
  border: none;
  box-shadow: 0 8rpx 20rpx rgba(200, 134, 13, 0.3);
  margin: 20rpx 0;
  position: relative;
  overflow: hidden;
}

.btn-primary::after {
  border: none;
}

.btn-primary:active {
  background: linear-gradient(135deg, #B8760C 0%, #CA9510 100%) !important;
  transform: scale(0.98);
  box-shadow: 0 4rpx 15rpx rgba(200, 134, 13, 0.5);
}

.btn-secondary {
  background: white !important;
  color: #C8860D !important;
  font-size: 34rpx;
  font-weight: 600;
  padding: 25rpx;
  border: 3rpx solid #C8860D;
  border-radius: 50rpx;
  box-shadow: 0 4rpx 15rpx rgba(200, 134, 13, 0.2);
  margin: 20rpx 0;
}

.btn-secondary::after {
  border: none;
}

.btn-secondary:active {
  background: #FFF8DC !important;
  transform: scale(0.98);
}

/* 单选按钮组 */
.radio-item {
  display: inline-block;
  margin-right: 40rpx;
  margin-top: 15rpx;
  font-size: 28rpx;
  color: #2F4F4F;
  font-weight: 500;
}

.radio-item radio {
  margin-right: 10rpx;
}

/* 快速功能区域 */
.quick-actions {
  display: flex;
  justify-content: center;
  margin-top: 20rpx;
}

.quick-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 30rpx 40rpx;
  background: linear-gradient(45deg, #F8F9FA 0%, #E9ECEF 100%);
  border-radius: 20rpx;
  min-width: 240rpx;
  transition: all 0.3s ease;
  box-shadow: 0 4rpx 15rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid #E0E0E0;
}

.quick-item:active {
  background: #F0F0F0;
  transform: scale(0.95);
}

.quick-icon {
  font-size: 60rpx;
  margin-bottom: 10rpx;
}

.quick-item text {
  font-size: 24rpx;
  color: #666;
  font-weight: 500;
}

/* 按钮禁用状态 */
.btn-primary[disabled] {
  background: #DDD !important;
  color: #999 !important;
  box-shadow: none !important;
}

.btn-primary[disabled]:active {
  transform: none !important;
}

/* 日期时间选择器优化 */
.picker[data-selected="true"] {
  color: #333;
  border-color: #C8860D;
  background: #FFF8DC;
}

/* 响应式布局优化 */
@media (max-width: 375px) {
  .container {
    padding: 30rpx 15rpx;
  }
  
  .title {
    font-size: 42rpx;
  }
  
  .quick-actions {
    flex-direction: column;
    gap: 20rpx;
  }
  
  .quick-item {
    min-width: auto;
    width: 100%;
  }
}

/* 加载状态优化 */
.calculating {
  opacity: 0.7;
  pointer-events: none;
}

/* 输入框焦点状态 */
.input-field:focus {
  border-color: #C8860D;
  box-shadow: 0 0 0 4rpx rgba(200, 134, 13, 0.1);
}

/* 动画效果 */
.card {
  animation: fadeInUp 0.6s ease-out;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 标签样式优化 */
.input-label {
  position: relative;
  color: #2F4F4F;
  font-weight: 600;
  font-size: 32rpx;
}

.input-label::after {
  content: '';
  position: absolute;
  bottom: -5rpx;
  left: 0;
  width: 40rpx;
  height: 4rpx;
  background: linear-gradient(90deg, #C8860D 0%, #DAA520 100%);
  border-radius: 2rpx;
}

/* 浮动动画 */
@keyframes float {
  0%, 100% {
    transform: translateY(0rpx);
  }
  50% {
    transform: translateY(-10rpx);
  }
}

/* 云朵浮动动画 */
@keyframes cloudFloat {
  0% {
    transform: translateX(-20rpx);
  }
  100% {
    transform: translateX(100vw);
  }
}

/* 主内容区域滑入动画 */
.main-content {
  animation: slideInUp 0.6s ease-out;
}

@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(50rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 响应式适配 */
@media (max-width: 375px) {
  .main-content {
    margin: -30rpx 15rpx 0;
    padding: 30rpx 20rpx;
  }
  
  .page-title {
    font-size: 42rpx;
  }
  
  .card {
    padding: 20rpx;
  }
}

/* 日期类型提示样式 */
.date-type-hint {
  font-size: 24rpx;
  color: #999;
  margin-top: 10rpx;
  padding-left: 10rpx;
  font-style: italic;
}

/* 农历日期选择器样式 */
.lunar-picker-container {
  margin-top: 10rpx;
}

.lunar-pickers {
  display: flex;
  justify-content: space-between;
  gap: 20rpx;
  margin-bottom: 10rpx;
}

.lunar-year-picker,
.lunar-month-picker,
.lunar-day-picker {
  flex: 1;
  min-width: 0;
}

.lunar-year-picker .picker,
.lunar-month-picker .picker,
.lunar-day-picker .picker {
  background: #F8F9FA;
  border: 2rpx solid #E0E0E0;
  border-radius: 12rpx;
  padding: 20rpx 15rpx;
  text-align: center;
  font-size: 28rpx;
  color: #333;
  min-height: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

.lunar-year-picker .picker:active,
.lunar-month-picker .picker:active,
.lunar-day-picker .picker:active {
  background: #E9ECEF;
  border-color: #C8860D;
  transform: scale(0.98);
}

/* 对应日期显示样式 */
.corresponding-date {
  margin-top: 8rpx;
  padding: 8rpx 12rpx;
  background: linear-gradient(90deg, #FFF8DC 0%, #F5DEB3 100%);
  border-left: 4rpx solid #C8860D;
  border-radius: 8rpx;
  font-size: 22rpx;
  color: #2F4F4F;
  font-weight: 500;
}

/* 日期选择器容器样式 */
.date-picker-container {
  margin-top: 10rpx;
}

.date-picker-container .picker {
  background: #F8F9FA;
  border: 2rpx solid #E0E0E0;
  border-radius: 12rpx;
  padding: 25rpx 20rpx;
  text-align: center;
  font-size: 32rpx;
  color: #333;
  transition: all 0.3s ease;
}

.date-picker-container .picker:active {
  background: #E9ECEF;
  border-color: #C8860D;
  transform: scale(0.98);
}

/* 输入组样式增强 */
.input-group {
  margin-bottom: 30rpx;
}

.input-group:last-child {
  margin-bottom: 0;
}

/* 选择器标签样式 */
.picker[placeholder] {
  color: #999;
}

/* 响应式适配农历选择器 */
@media (max-width: 375px) {
  .lunar-pickers {
    gap: 10rpx;
  }
  
  .lunar-year-picker .picker,
  .lunar-month-picker .picker,
  .lunar-day-picker .picker {
    padding: 15rpx 10rpx;
    font-size: 24rpx;
  }
  
  .corresponding-date {
    font-size: 20rpx;
    padding: 6rpx 10rpx;
  }
}

/* 每日运势卡片样式 */
.daily-fortune-card {
  background: linear-gradient(135deg, #FFF8DC 0%, #F0F8FF 100%);
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  box-shadow: 0 6rpx 20rpx rgba(200, 134, 13, 0.15);
  border: 2rpx solid #E6E6FA;
  position: relative;
  overflow: hidden;
}

.daily-fortune-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(90deg, #C8860D 0%, #DAA520 50%, #FF6B6B 100%);
}

/* 运势卡片头部 */
.fortune-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 25rpx;
  padding-bottom: 15rpx;
  border-bottom: 1rpx solid #E0E0E0;
}

.fortune-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #2F4F4F;
}

.fortune-date {
  font-size: 24rpx;
  color: #666;
  background: #FFF;
  padding: 8rpx 15rpx;
  border-radius: 15rpx;
  border: 1rpx solid #E0E0E0;
}

/* 通用运势样式 */
.universal-fortune {
  margin-bottom: 25rpx;
}

.fortune-section {
  background: #FFF;
  border-radius: 15rpx;
  padding: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

.fortune-score {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 15rpx;
}

.score-label {
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
}

.stars {
  display: flex;
  gap: 4rpx;
}

.star {
  font-size: 24rpx;
  color: #DDD;
  transition: color 0.3s ease;
}

.star.filled {
  color: #FFD700;
  text-shadow: 0 0 4rpx rgba(255, 215, 0, 0.6);
}

.fortune-details {
  display: flex;
  gap: 20rpx;
  flex-wrap: wrap;
}

.detail-item {
  font-size: 24rpx;
  color: #666;
  background: #F8F9FA;
  padding: 8rpx 12rpx;
  border-radius: 12rpx;
  border: 1rpx solid #E9ECEF;
}

/* 个人专属运势样式 */
.personal-fortunes {
  margin-bottom: 25rpx;
}

.fortune-section-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #2F4F4F;
  margin-bottom: 15rpx;
  padding-left: 10rpx;
  border-left: 4rpx solid #C8860D;
}

.personal-fortune-item {
  background: #FFF;
  border-radius: 15rpx;
  padding: 20rpx;
  margin-bottom: 15rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  border: 1rpx solid #F0F0F0;
}

.personal-fortune-item:last-child {
  margin-bottom: 0;
}

.personal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 15rpx;
}

.person-name {
  font-size: 32rpx;
  font-weight: bold;
  color: #2c3e50;
}

.person-actions {
  display: flex;
  align-items: center;
  gap: 15rpx;
}

.person-score {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.member-menu-btn {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  background-color: #f0f0f0;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  color: #666;
  padding: 0;
  margin: 0;
  line-height: 1;
}

.member-menu-btn:hover {
  background-color: #e0e0e0;
}

.member-menu-btn::after {
  border: none;
}

.person-score {
  display: flex;
  gap: 3rpx;
}

.personal-details {
  margin-top: 15rpx;
}

.score-row {
  display: flex;
  gap: 15rpx;
  margin-bottom: 12rpx;
  flex-wrap: wrap;
}

.score-item {
  font-size: 22rpx;
  color: #666;
  background: #F8F9FA;
  padding: 6rpx 10rpx;
  border-radius: 10rpx;
  border: 1rpx solid #E9ECEF;
  flex: 1;
  text-align: center;
  min-width: 80rpx;
}

.suggestions {
  margin-bottom: 10rpx;
}

.suggestion-item {
  display: inline-block;
  font-size: 22rpx;
  color: #28A745;
  background: #F8FFF9;
  padding: 6rpx 10rpx;
  border-radius: 10rpx;
  margin-right: 8rpx;
  margin-bottom: 6rpx;
  border: 1rpx solid #E8F5E8;
}

.warnings {
  margin-bottom: 10rpx;
}

.warning-item {
  display: inline-block;
  font-size: 22rpx;
  color: #DC3545;
  background: #FFF5F5;
  padding: 6rpx 10rpx;
  border-radius: 10rpx;
  margin-right: 8rpx;
  margin-bottom: 6rpx;
  border: 1rpx solid #F5E6E8;
}

/* 提示信息样式 */
.fortune-hint {
  text-align: center;
  padding: 30rpx 20rpx;
  background: #F8F9FA;
  border-radius: 15rpx;
  border: 2rpx dashed #DDD;
  margin-bottom: 25rpx;
}

.hint-text {
  font-size: 26rpx;
  color: #666;
  font-style: italic;
}

/* 运势操作按钮样式 */
.fortune-actions {
  display: flex;
  gap: 15rpx;
  justify-content: center;
  margin-top: 20rpx;
}

.action-btn {
  background: linear-gradient(135deg, #C8860D 0%, #DAA520 100%) !important;
  color: white !important;
  font-size: 24rpx;
  font-weight: 600;
  padding: 16rpx 30rpx !important;
  border-radius: 25rpx;
  border: none;
  box-shadow: 0 4rpx 12rpx rgba(200, 134, 13, 0.3);
  min-width: 120rpx;
  line-height: 1.2;
}

.action-btn::after {
  border: none;
}

.action-btn:active {
  background: linear-gradient(135deg, #B8760C 0%, #CA9510 100%) !important;
  transform: scale(0.96);
  box-shadow: 0 2rpx 8rpx rgba(200, 134, 13, 0.5);
}

/* 响应式适配 */
@media (max-width: 375px) {
  .daily-fortune-card {
    padding: 20rpx;
    margin-bottom: 20rpx;
  }
  
  .fortune-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 10rpx;
  }
  
  .fortune-title {
    font-size: 28rpx;
  }
  
  .personal-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 8rpx;
  }
  
  .score-row {
    gap: 8rpx;
  }
  
  .score-item {
    font-size: 20rpx;
    padding: 4rpx 8rpx;
    min-width: 70rpx;
  }
  
  .fortune-actions {
    flex-direction: column;
    gap: 10rpx;
  }
  
  .action-btn {
    width: 100%;
  }
}

/* 动画效果 */
.daily-fortune-card {
  animation: fadeInSlide 0.6s ease-out;
}

@keyframes fadeInSlide {
  from {
    opacity: 0;
    transform: translateY(-20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.personal-fortune-item {
  animation: itemSlideIn 0.4s ease-out;
}

@keyframes itemSlideIn {
  from {
    opacity: 0;
    transform: translateX(-15rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* 星星闪烁动画 */
.star.filled {
  animation: starTwinkle 2s ease-in-out infinite;
}

@keyframes starTwinkle {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.8;
    transform: scale(1.1);
  }
}

/* 新增：家庭运势概览样式 */
.family-overview {
  background: linear-gradient(135deg, #FFF8DC 0%, #F0F8FF 100%);
  border-radius: 20rpx;
  padding: 25rpx;
  margin-bottom: 25rpx;
  border: 2rpx solid #E6E6FA;
  box-shadow: 0 4rpx 15rpx rgba(0, 0, 0, 0.08);
}

.family-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
  padding-bottom: 15rpx;
  border-bottom: 1rpx solid #E0E0E0;
}

.family-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #2F4F4F;
}

.family-stats {
  display: flex;
  align-items: center;
  gap: 10rpx;
  font-size: 22rpx;
  color: #666;
}

.stat-item {
  background: #FFF;
  padding: 6rpx 12rpx;
  border-radius: 12rpx;
  border: 1rpx solid #E0E0E0;
}

.stat-separator {
  color: #CCC;
}

.family-score-display {
  display: flex;
  align-items: center;
  gap: 25rpx;
  margin-bottom: 20rpx;
}

.score-circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 120rpx;
  height: 120rpx;
  background: linear-gradient(135deg, #C8860D 0%, #DAA520 100%);
  border-radius: 50%;
  color: white;
  box-shadow: 0 6rpx 20rpx rgba(200, 134, 13, 0.3);
}

.score-number {
  font-size: 36rpx;
  font-weight: bold;
  line-height: 1;
}

.score-label {
  font-size: 20rpx;
  opacity: 0.9;
  margin-top: 4rpx;
}

.family-details {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.family-lucky, .active-members {
  font-size: 24rpx;
  color: #666;
  padding: 8rpx 15rpx;
  background: #FFF;
  border-radius: 12rpx;
  border: 1rpx solid #E9ECEF;
}

.family-suggestions {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.family-suggestions .suggestion-item {
  display: block;
  font-size: 22rpx;
  color: #28A745;
  background: #F8FFF9;
  padding: 8rpx 12rpx;
  border-radius: 12rpx;
  border: 1rpx solid #E8F5E8;
  line-height: 1.4;
}

/* 新增：加载状态样式 */
.fortune-loading {
  text-align: center;
  padding: 40rpx 20rpx;
  color: #666;
}

.loading-text {
  font-size: 26rpx;
  color: #C8860D;
  font-weight: 500;
}

/* 新增：更新时间样式 */
.fortune-update {
  margin-top: 8rpx;
}

.update-time {
  font-size: 20rpx;
  color: #999;
  background: #F8F9FA;
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
}

/* 离线模式指示器样式 */
.offline-indicator {
  margin-top: 8rpx;
}

.offline-text {
  font-size: 20rpx;
  color: #FF6B6B;
  background: #FFF5F5;
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
  border: 1rpx solid #FFE6E6;
}

/* 新增：增强的个人运势列表样式 */
.fortune-section-title {
  display: flex;
  align-items: center;
  gap: 10rpx;
  font-size: 28rpx;
  font-weight: bold;
  color: #2F4F4F;
  margin-bottom: 15rpx;
  padding-left: 10rpx;
  border-left: 4rpx solid #C8860D;
}

.section-title {
  flex: 1;
}

.members-count {
  font-size: 22rpx;
  color: #666;
  font-weight: normal;
  background: #FFF;
  padding: 4rpx 10rpx;
  border-radius: 10rpx;
  border: 1rpx solid #E0E0E0;
}

/* 新增：评分文本样式 */
.score-text {
  font-size: 20rpx;
  color: #666;
  margin-left: 8rpx;
  font-weight: 500;
}

/* 新增：幸运元素样式 */
.lucky-elements {
  display: flex;
  gap: 10rpx;
  margin-bottom: 10rpx;
  flex-wrap: wrap;
}

.lucky-item {
  font-size: 22rpx;
  color: #C8860D;
  background: #FFF8DC;
  padding: 6rpx 12rpx;
  border-radius: 12rpx;
  border: 1rpx solid #F0E68C;
  font-weight: 500;
}

/* 新增：运势错误状态样式 */
.fortune-error {
  text-align: center;
  padding: 15rpx;
  background: #FFF5F5;
  border-radius: 10rpx;
  border: 1rpx solid #F5E6E8;
}

.error-text {
  font-size: 22rpx;
  color: #DC3545;
}


/* 新增：增强的提示信息样式 */
.hint-content {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  align-items: center;
}

.hint-desc {
  font-size: 22rpx;
  color: #999;
  font-style: normal;
}

/* 新增：按钮状态样式 */
.action-btn.primary {
  background: linear-gradient(135deg, #C8860D 0%, #DAA520 100%) !important;
}

.action-btn.secondary {
  background: white !important;
  color: #C8860D !important;
  border: 2rpx solid #C8860D;
}

.action-btn.secondary:active {
  background: #FFF8DC !important;
}

.action-btn[disabled] {
  background: #DDD !important;
  color: #999 !important;
  box-shadow: none !important;
  border-color: #DDD !important;
}

.action-btn[disabled]:active {
  transform: none !important;
}


/* 平衡型运势卡片 */
.balanced-fortune-card {
  background: linear-gradient(135deg, #ffffff 0%, #fafbfc 100%);
  border-radius: 16rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid #e8f0fe;
  overflow: hidden;
  transition: all 0.3s ease;
  animation: balancedCardSlideIn 0.5s ease-out;
}

.balanced-fortune-card:active {
  transform: translateY(2rpx);
  box-shadow: 0 6rpx 25rpx rgba(0, 0, 0, 0.12);
}

/* 旧版本保留以防兼容性 */
.compact-fortune-card {
  background: #ffffff;
  border-radius: 12rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
  border: 1rpx solid #f0f0f0;
  overflow: hidden;
  transition: all 0.3s ease;
}

.compact-fortune-card:active {
  transform: translateY(1rpx);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
}

/* 平衡型卡片头部 */
.balanced-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 28rpx;
  background: linear-gradient(135deg, #4c6ef5 0%, #748ffc 50%, #91a7ff 100%);
  color: white;
  position: relative;
  overflow: hidden;
}

.balanced-header::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  animation: balancedShimmer 4s infinite;
}

.member-section {
  display: flex;
  align-items: center;
  gap: 20rpx;
  z-index: 1;
}

.member-avatar {
  width: 70rpx;
  height: 70rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: bold;
  color: white;
  border: 3rpx solid rgba(255, 255, 255, 0.3);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.2);
}

.member-info {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.member-name {
  font-size: 32rpx;
  font-weight: bold;
  color: white;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.3);
  line-height: 1.2;
}

.member-age {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.9);
  background: rgba(255, 255, 255, 0.1);
  padding: 4rpx 10rpx;
  border-radius: 10rpx;
  align-self: flex-start;
  line-height: 1.2;
}

.score-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  z-index: 1;
}

.score-number {
  font-size: 42rpx;
  font-weight: bold;
  color: #ffd700;
  text-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.3);
  line-height: 1;
}

.score-stars {
  display: flex;
  gap: 4rpx;
}

.score-stars .star {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.4);
  transition: color 0.3s ease;
}

.score-stars .star.filled {
  color: #ffd700;
  text-shadow: 0 0 8rpx rgba(255, 215, 0, 0.8);
  animation: starGlow 2s ease-in-out infinite alternate;
}

/* 保留旧版本样式 */
.compact-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 20rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
}

.member-basic {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

/* 平衡型运势详情 */
.balanced-details {
  padding: 28rpx 30rpx;
}

.scores-cards {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 12rpx;
  margin-bottom: 24rpx;
}

.score-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 16rpx 8rpx;
  background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
  border-radius: 12rpx;
  border: 2rpx solid #e3f2fd;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  transition: all 0.3s ease;
}

.score-card:active {
  transform: translateY(1rpx);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  border-color: #bbdefb;
}

.score-icon {
  font-size: 28rpx;
  line-height: 1;
}

.score-label {
  font-size: 22rpx;
  color: #37474f;
  font-weight: 600;
  line-height: 1.2;
}

.score-value {
  font-size: 26rpx;
  font-weight: bold;
  color: #1565c0;
  line-height: 1;
}

/* 保留旧版本样式 */
.compact-details {
  padding: 15rpx 20rpx;
}

.scores-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 8rpx;
  margin-bottom: 12rpx;
}

.score-cell {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
  padding: 8rpx 4rpx;
  background: #f8f9fa;
  border-radius: 8rpx;
  border: 1rpx solid #e9ecef;
}

.score-text {
  font-size: 18rpx;
  font-weight: bold;
  color: #495057;
  line-height: 1;
}

/* 平衡型幸运信息 */
.lucky-section {
  display: flex;
  gap: 12rpx;
  margin-bottom: 24rpx;
  flex-wrap: wrap;
}

.lucky-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
  padding: 12rpx 16rpx;
  background: linear-gradient(135deg, #fff8e1 0%, #ffecb3 100%);
  border-radius: 12rpx;
  border: 2rpx solid #ffc107;
  flex: 1;
  min-width: 0;
  box-shadow: 0 2rpx 8rpx rgba(255, 193, 7, 0.15);
  text-align: center;
  transition: all 0.3s ease;
}

.lucky-card:active {
  transform: translateY(1rpx);
  box-shadow: 0 4rpx 12rpx rgba(255, 193, 7, 0.25);
}

.lucky-label {
  font-size: 22rpx;
  color: #f57c00;
  font-weight: 600;
  white-space: nowrap;
  line-height: 1.2;
}

.lucky-value {
  font-size: 24rpx;
  font-weight: bold;
  color: #ef6c00;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  width: 100%;
  line-height: 1.2;
}

/* 保留旧版本样式 */
.lucky-row {
  display: flex;
  gap: 12rpx;
  margin-bottom: 12rpx;
  flex-wrap: wrap;
  justify-content: space-around;
}

.lucky-item {
  font-size: 18rpx;
  color: #f57c00;
  background: #fff8e1;
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
  border: 1rpx solid #ffcc02;
  font-weight: 500;
  line-height: 1.2;
  white-space: nowrap;
}

/* 五行分析 - 紧凑布局，减少空白 */
.wuxing-section {
  width: 100%;
  margin-bottom: 15rpx;
  padding: 15rpx;
  background: linear-gradient(135deg, #e8f5e8 0%, #f1f8e9 100%);
  border-radius: 12rpx;
  border: 1rpx solid #c8e6c9;
  clear: both;
  box-sizing: border-box;
}

.section-title {
  font-size: 24rpx;
  font-weight: bold;
  color: #2e7d32;
  margin-bottom: 8rpx;
  position: relative;
  padding-left: 16rpx;
  width: 100%;
  display: block;
}

.section-title::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 6rpx;
  height: 16rpx;
  background: linear-gradient(135deg, #4caf50 0%, #66bb6a 100%);
  border-radius: 3rpx;
}

.wuxing-info {
  width: 100%;
  display: block;
}

.wuxing-text {
  font-size: 22rpx;
  color: #388e3c;
  width: 100%;
  display: block;
  line-height: 1.4;
}

/* 平衡型建议区域 */
.suggestions-section {
  margin-bottom: 20rpx;
}

.suggestions-section .section-title {
  font-size: 24rpx;
  font-weight: bold;
  color: #2e7d32;
  margin-bottom: 12rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.suggestions-list {
  display: flex;
  gap: 8rpx;
  flex-wrap: wrap;
}

.suggestion-tag {
  font-size: 22rpx;
  color: #2e7d32;
  background: linear-gradient(135deg, #e8f5e8 0%, #c8e6c9 100%);
  padding: 8rpx 12rpx;
  border-radius: 12rpx;
  border: 1rpx solid #a5d6a7;
  font-weight: 500;
  line-height: 1.2;
  transition: all 0.3s ease;
}

.suggestion-tag:active {
  transform: translateY(1rpx);
  box-shadow: 0 2rpx 6rpx rgba(46, 125, 50, 0.2);
}

/* 保留旧版本样式 */
.suggestions-compact {
  display: flex;
  gap: 6rpx;
  flex-wrap: wrap;
  margin-bottom: 8rpx;
}

.suggestion-compact {
  font-size: 16rpx;
  color: #2e7d32;
  background: #e8f5e8;
  padding: 3rpx 8rpx;
  border-radius: 8rpx;
  border: 1rpx solid #c8e6c9;
  font-weight: 500;
  line-height: 1.2;
}

/* 年运提示 */
.yearly-fortune {
  margin-bottom: 20rpx;
}

.fortune-tip {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
  padding: 16rpx 20rpx;
  background: linear-gradient(135deg, #fff3e0 0%, #ffe0b2 100%);
  border-radius: 16rpx;
  border: 1rpx solid #ffcc02;
}

.tip-icon {
  font-size: 24rpx;
  margin-top: 2rpx;
}

.tip-text {
  font-size: 24rpx;
  color: #e65100;
  line-height: 1.4;
  font-weight: 500;
}

/* 紧凑型操作按钮 */
.compact-actions {
  display: flex;
  gap: 8rpx;
  padding: 12rpx 20rpx;
  background: #f8f9fa;
  border-top: 1rpx solid #e9ecef;
}

.compact-btn {
  flex: 1;
  padding: 8rpx 12rpx !important;
  border-radius: 8rpx;
  border: none;
  font-size: 20rpx;
  font-weight: 600;
  transition: all 0.3s ease;
  line-height: 1.2;
}

.compact-btn::after {
  border: none;
}

.compact-btn.primary {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
  color: white !important;
  box-shadow: 0 2rpx 8rpx rgba(102, 126, 234, 0.3);
}

.compact-btn.secondary {
  background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%) !important;
  color: #e65100 !important;
  box-shadow: 0 2rpx 8rpx rgba(252, 182, 159, 0.3);
}

.compact-btn.danger {
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%) !important;
  color: white !important;
  box-shadow: 0 2rpx 8rpx rgba(255, 107, 107, 0.3);
}

.compact-btn:active {
  transform: translateY(1rpx);
  box-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.2);
}

/* 错误状态 */
.error-state {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  padding: 30rpx;
  background: linear-gradient(135deg, #fff5f5 0%, #ffe0e0 100%);
  border-radius: 16rpx;
  border: 1rpx solid #ffcdd2;
}

.error-icon {
  font-size: 32rpx;
  color: #f44336;
}

.error-text {
  font-size: 24rpx;
  color: #d32f2f;
  font-weight: 500;
}

/* 响应式适配 - 紧凑型卡片 */
@media (max-width: 375px) {
  .compact-fortune-card {
    margin-bottom: 12rpx;
  }
  
  .compact-header {
    padding: 12rpx 16rpx;
  }
  
  .member-basic {
    gap: 8rpx;
  }
  
  .member-avatar {
    width: 40rpx;
    height: 40rpx;
    font-size: 16rpx;
  }
  
  .member-name {
    font-size: 24rpx;
  }
  
  .member-age {
    font-size: 18rpx;
  }
  
  .score-display {
    gap: 2rpx;
  }
  
  .score-number {
    font-size: 28rpx;
  }
  
  .score-stars .star {
    font-size: 14rpx;
    gap: 1rpx;
  }
  
  .compact-details {
    padding: 12rpx 16rpx;
  }
  
  .scores-grid {
    gap: 6rpx;
    margin-bottom: 10rpx;
  }
  
  .score-cell {
    padding: 6rpx 2rpx;
    gap: 2rpx;
  }
  
  .score-icon {
    font-size: 16rpx;
  }
  
  .score-text {
    font-size: 14rpx;
  }
  
  .lucky-row {
    gap: 8rpx;
    margin-bottom: 10rpx;
  }
  
  .lucky-item {
    font-size: 14rpx;
    padding: 2rpx 6rpx;
  }
  
  .suggestions-compact {
    gap: 4rpx;
    margin-bottom: 6rpx;
  }
  
  .suggestion-compact {
    font-size: 14rpx;
    padding: 2rpx 6rpx;
  }
  
  .compact-actions {
    padding: 8rpx 16rpx;
    gap: 6rpx;
  }
  
  .compact-btn {
    padding: 6rpx 8rpx !important;
    font-size: 18rpx;
  }
}

/* 响应式适配 - 现代卡片 */
@media (max-width: 375px) {
  .modern-fortune-card {
    margin-bottom: 20rpx;
  }
  
  .card-header {
    padding: 20rpx;
  }
  
  .member-info {
    gap: 15rpx;
  }
  
  .member-avatar {
    width: 60rpx;
    height: 60rpx;
    font-size: 24rpx;
  }
  
  .member-name {
    font-size: 28rpx;
  }
  
  .member-age {
    font-size: 20rpx;
    padding: 4rpx 8rpx;
  }
  
  .score-number {
    font-size: 36rpx;
  }
  
  .score-stars .star {
    font-size: 16rpx;
  }
  
  .fortune-details {
    padding: 20rpx;
  }
  
  .detailed-scores {
    grid-template-columns: 1fr;
    gap: 15rpx;
    margin-bottom: 20rpx;
  }
  
  .score-item {
    padding: 15rpx;
    gap: 10rpx;
  }
  
  .score-icon {
    font-size: 28rpx;
    width: 35rpx;
  }
  
  .score-label {
    font-size: 22rpx;
  }
  
  .score-value {
    font-size: 28rpx;
  }
  
  .lucky-section {
    gap: 10rpx;
    flex-direction: column;
  }
  
  .lucky-item {
    padding: 10rpx 12rpx;
    gap: 6rpx;
  }
  
  .lucky-label {
    font-size: 20rpx;
  }
  
  .lucky-value {
    font-size: 22rpx;
  }
  
  .wuxing-section {
    padding: 15rpx;
    margin-bottom: 20rpx;
  }
  
  .section-title {
    font-size: 24rpx;
    margin-bottom: 10rpx;
    padding-left: 15rpx;
  }
  
  .section-title::before {
    width: 6rpx;
    height: 16rpx;
  }
  
  .wuxing-info {
    flex-direction: column;
    align-items: flex-start;
    gap: 8rpx;
  }
  
  .wuxing-text {
    font-size: 22rpx;
  }
  
  .harmony-score {
    font-size: 20rpx;
    padding: 4rpx 8rpx;
  }
  
  .suggestions-list {
    gap: 8rpx;
  }
  
  .suggestion-tag {
    font-size: 20rpx;
    padding: 6rpx 12rpx;
  }
  
  .fortune-tip {
    padding: 12rpx 15rpx;
    gap: 8rpx;
  }
  
  .tip-icon {
    font-size: 20rpx;
  }
  
  .tip-text {
    font-size: 22rpx;
  }
  
  .card-actions {
    padding: 20rpx;
    gap: 10rpx;
  }
  
  .action-btn {
    padding: 15rpx 10rpx !important;
    gap: 6rpx;
  }
  
  .btn-icon {
    font-size: 20rpx;
  }
  
  .btn-text {
    font-size: 20rpx;
  }
  
  .error-state {
    padding: 20rpx;
    gap: 8rpx;
  }
  
  .error-icon {
    font-size: 28rpx;
  }
  
  .error-text {
    font-size: 22rpx;
  }
}

/* 动画增强 */
.compact-fortune-card {
  animation: compactCardSlideIn 0.4s ease-out;
}

@keyframes compactCardSlideIn {
  from {
    opacity: 0;
    transform: translateY(10rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.modern-fortune-card {
  animation: cardSlideIn 0.5s ease-out;
}

@keyframes cardSlideIn {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 紧凑型卡片头部光效 */
.compact-header {
  position: relative;
  overflow: hidden;
}

.compact-header::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.15), transparent);
  animation: compactShimmer 4s infinite;
}

@keyframes compactShimmer {
  0% {
    left: -100%;
  }
  100% {
    left: 100%;
  }
}

.card-header {
  position: relative;
  overflow: hidden;
}

.card-header::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  animation: shimmer 3s infinite;
}

@keyframes shimmer {
  0% {
    left: -100%;
  }
  100% {
    left: 100%;
  }
}

.score-stars .star.filled {
  animation: starGlow 2s ease-in-out infinite alternate;
}

@keyframes starGlow {
  from {
    text-shadow: 0 0 8rpx rgba(255, 215, 0, 0.8);
  }
  to {
    text-shadow: 0 0 16rpx rgba(255, 215, 0, 1), 0 0 24rpx rgba(255, 215, 0, 0.5);
  }
}

/* 平衡型操作按钮 */
.balanced-actions {
  display: flex;
  gap: 12rpx;
  padding: 20rpx 28rpx;
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  border-top: 1rpx solid #dee2e6;
}

.balanced-actions .action-btn {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
  padding: 16rpx 8rpx !important;
  border-radius: 12rpx;
  border: none;
  font-size: 22rpx;
  font-weight: 600;
  transition: all 0.3s ease;
  line-height: 1.2;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
  text-align: center;
  min-height: 80rpx;
}

.balanced-actions .action-btn::after {
  border: none;
}

.balanced-actions .action-btn.primary {
  background: linear-gradient(135deg, #4c6ef5 0%, #748ffc 100%) !important;
  color: white !important;
  box-shadow: 0 3rpx 12rpx rgba(76, 110, 245, 0.3);
}

.balanced-actions .action-btn.secondary {
  background: linear-gradient(135deg, #fff3e0 0%, #ffe0b2 100%) !important;
  color: #ef6c00 !important;
  border: 2rpx solid #ffb74d;
  box-shadow: 0 3rpx 12rpx rgba(255, 183, 77, 0.3);
}

.balanced-actions .action-btn.danger {
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%) !important;
  color: white !important;
  box-shadow: 0 3rpx 12rpx rgba(255, 107, 107, 0.3);
}

.balanced-actions .action-btn:active {
  transform: translateY(2rpx);
  box-shadow: 0 1rpx 6rpx rgba(0, 0, 0, 0.2);
}

.btn-icon {
  font-size: 24rpx;
  line-height: 1;
}

.btn-text {
  font-size: 22rpx;
  font-weight: 600;
  line-height: 1.2;
}

/* 动画效果 */
@keyframes balancedCardSlideIn {
  from {
    opacity: 0;
    transform: translateY(15rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes balancedShimmer {
  0% {
    left: -100%;
  }
  100% {
    left: 100%;
  }
}

@keyframes starGlow {
  from {
    text-shadow: 0 0 8rpx rgba(255, 215, 0, 0.8);
  }
  to {
    text-shadow: 0 0 16rpx rgba(255, 215, 0, 1), 0 0 24rpx rgba(255, 215, 0, 0.5);
  }
}

/* 响应式适配 - 平衡型卡片 */
@media (max-width: 375px) {
  .balanced-fortune-card {
    margin-bottom: 20rpx;
  }
  
  .balanced-header {
    padding: 20rpx 24rpx;
  }
  
  .member-section {
    gap: 16rpx;
  }
  
  .member-avatar {
    width: 60rpx;
    height: 60rpx;
    font-size: 24rpx;
  }
  
  .member-name {
    font-size: 28rpx;
  }
  
  .member-age {
    font-size: 20rpx;
    padding: 3rpx 8rpx;
  }
  
  .score-section {
    gap: 6rpx;
  }
  
  .score-number {
    font-size: 36rpx;
  }
  
  .score-stars .star {
    font-size: 18rpx;
    gap: 3rpx;
  }
  
  .balanced-details {
    padding: 24rpx 26rpx;
  }
  
  .scores-cards {
    gap: 10rpx;
    margin-bottom: 20rpx;
  }
  
  .score-card {
    padding: 14rpx 6rpx;
    gap: 6rpx;
  }
  
  .score-icon {
    font-size: 24rpx;
  }
  
  .score-label {
    font-size: 20rpx;
  }
  
  .score-value {
    font-size: 24rpx;
  }
  
  .lucky-section {
    gap: 10rpx;
    margin-bottom: 20rpx;
  }
  
  .lucky-card {
    padding: 10rpx 14rpx;
    gap: 5rpx;
  }
  
  .lucky-label {
    font-size: 20rpx;
  }
  
  .lucky-value {
    font-size: 22rpx;
  }
  
  .suggestions-section {
    margin-bottom: 16rpx;
  }
  
  .suggestions-section .section-title {
    font-size: 22rpx;
    margin-bottom: 10rpx;
    gap: 6rpx;
  }
  
  .suggestions-list {
    gap: 6rpx;
  }
  
  .suggestion-tag {
    font-size: 20rpx;
    padding: 6rpx 10rpx;
  }
  
  .balanced-actions {
    padding: 16rpx 24rpx;
    gap: 10rpx;
  }
  
  .balanced-actions .action-btn {
    padding: 14rpx 6rpx !important;
    gap: 5rpx;
    min-height: 70rpx;
  }
  
  .btn-icon {
    font-size: 22rpx;
  }
  
  .btn-text {
    font-size: 20rpx;
  }
}

/* 响应式适配 - 家庭运势 */
@media (max-width: 375px) {
  .family-score-display {
    flex-direction: column;
    align-items: center;
    gap: 15rpx;
    text-align: center;
  }
  
  .score-circle {
    width: 100rpx;
    height: 100rpx;
  }
  
  .score-number {
    font-size: 30rpx;
  }
  
  .family-details {
    width: 100%;
  }
  
  .family-stats {
    flex-direction: column;
    gap: 6rpx;
  }
  
  .fortune-section-title {
    flex-direction: column;
    align-items: flex-start;
    gap: 6rpx;
  }
  
  .lucky-elements {
    justify-content: center;
  }
}
